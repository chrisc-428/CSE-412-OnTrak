const { Pool } = require('pg');

const pool = new Pool({
  user: 'username',
  host: 'localhost',
  database: 'database-name',
  password: 'password',
  port: 5432,
});

module.exports = pool;